import Link from 'next/link'; import { ArrowRight, MailCheck } from 'lucide-react'; import { AuthShell, authStyles as s } from '@/components/auth/AuthShell';
export default function VerifyEmail(){return <AuthShell eyebrow="Verify your identity" title="Check your inbox" subtitle="We sent a six-digit verification code to your work email."><div style={{width:54,height:54,borderRadius:15,background:'var(--primary-tint)',color:'var(--primary)',display:'grid',placeItems:'center'}}><MailCheck/></div><div className={s.code}>{[1,2,3,4,5,6].map(x=><input key={x} inputMode="numeric" maxLength={1}/>)}</div><Link href="/company/register" className={s.primaryButton}>Verify and continue <ArrowRight size={16}/></Link><div className={s.switch}>Didn’t receive it? <a href="#">Resend code</a></div></AuthShell>}
